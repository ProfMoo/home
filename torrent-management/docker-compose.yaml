version: "3.9"
services:
  torrent-management:
    build: .
    environment:
      REDACTED_API_KEY: "edb12004.3cbc020a92da8d3aa4e70e2df373b8c6"
      REDACTED_ANNOUNCE_URL: "https://flacsfor.me/7ff5fc52d1dff7075d7dbd7d2c393b6a/announce"
      MUSIC_TO_UPLOAD_DIR: "/uploads"
      TORRENT_DIR: "/torrent-files"
    # restart: unless-stopped
    volumes:
      - /mnt/x/uploads:/uploads
      - /mnt/x/torrent-files:/torrent-files
    command: [ "main.py" ]
