# docker compose up
version: "3.8"
services:
  beets:
    build: .
    environment:
      - PUID=1000
      - PGID=1000
      - BEETSDIR=.
    volumes:
      - ./volume:/volume
    ports:
      - 8337:8337
    restart: unless-stopped
    entrypoint: /entrypoint.sh
