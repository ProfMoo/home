# docker compose up
version: "3.8"
services:
  beets:
    build: .
    environment:
      - PUID=1000
      - PGID=1000
    volumes:
      - ./volume:/volume
      - /mnt/x/test:/music
      - /mnt/c/Users/DrMoo/Music/test:/downloads
    ports:
      - 8337:8337
    restart: unless-stopped
    entrypoint: /entrypoint.sh
