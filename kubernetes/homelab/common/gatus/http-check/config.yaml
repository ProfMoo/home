---
endpoints:
  - name: "${APP}"
    group: http-check
    url: "https://${GATUS_SUBDOMAIN:=${APP}}.drmoo.io${GATUS_PATH:=/}"
    interval: 1m
    client:
      dns-resolver: tcp://1.1.1.1:53
    conditions:
      - "[STATUS] == ${GATUS_STATUS:=200}"
