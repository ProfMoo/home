apiVersion: v1
kind: Service
metadata:
  name: prowlarr-gui
spec:
  selector:
    app.kubernetes.io/name: prowlarr
  ports:
    - protocol: TCP
      name: gui
      port: 80
      targetPort: 80
      nodePort: 30903
  type: NodePort
