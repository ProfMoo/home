apiVersion: v1
kind: Service
metadata:
  name: radarr-gui
spec:
  selector:
    app.kubernetes.io/name: raddar
  ports:
    - protocol: TCP
      name: gui
      port: 80
      targetPort: 80
      nodePort: 30902
  type: NodePort
