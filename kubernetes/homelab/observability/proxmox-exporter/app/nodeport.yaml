apiVersion: v1
kind: Service
metadata:
  name: proxmox
spec:
  selector:
    app.kubernetes.io/name: proxmox-exporter
  ports:
    - protocol: TCP
      name: exporter
      port: 9221
      targetPort: 9221
      nodePort: 30089
  type: NodePort
