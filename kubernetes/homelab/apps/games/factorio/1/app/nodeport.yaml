apiVersion: v1
kind: Service
metadata:
  name: factorio-1
spec:
  selector:
    app.kubernetes.io/instance: factorio-1-factorio
  ports:
    - protocol: TCP
      name: gui
      port: 25565
      targetPort: 25565
      nodePort: 30200
  type: NodePort
