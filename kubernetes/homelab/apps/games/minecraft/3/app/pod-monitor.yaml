---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/monitoring.coreos.com/podmonitor_v1.json
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: minecraft-3
spec:
  selector:
    matchLabels:
      app.kubernetes.io/instance: minecraft-3
  podMetricsEndpoints:
    - targetPort: 25585
      path: /metrics
      interval: 30s
