apiVersion: v1
kind: Service
metadata:
  name: sonarr-gui
spec:
  selector:
    app.kubernetes.io/name: sonarr
  ports:
    - protocol: TCP
      name: gui
      port: 80
      targetPort: 80
      nodePort: 30904
  type: NodePort
