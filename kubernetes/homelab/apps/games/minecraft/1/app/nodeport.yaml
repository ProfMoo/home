apiVersion: v1
kind: Service
metadata:
  name: minecraft
spec:
  selector:
    app.kubernetes.io/name: minecraft
  ports:
    - protocol: TCP
      name: gui
      port: 25565
      targetPort: 25565
      nodePort: 25565
  type: NodePort
