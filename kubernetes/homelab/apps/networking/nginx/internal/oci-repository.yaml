---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/source.toolkit.fluxcd.io/ocirepository_v1.json
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: nginx-internal
spec:
  interval: 5m
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    tag: 4.13.2
  # TODO: Use a more standard kubernetes-sig URL when it's available.
  # This seems to be some kinda half-supporter URL push
  # https://github.com/kubernetes/ingress-nginx/issues/13513
  url: oci://us-central1-docker.pkg.dev/k8s-staging-images/ingress-nginx/charts/ingress-nginx
