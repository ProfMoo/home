---
# NOTE: This is empty because we really only want the common volsync component in this fluxtomization.
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
