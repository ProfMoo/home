---
# yaml-language-server: $schema=https://kube-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
# GitHub App PEM stored as a Bitwarden secure note / field
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: &name renovate-bot-pem
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden-fields
  target:
    name: *name
  data:
    - secretKey: private_key_pem
      remoteRef:
        # TODO: replace with actual Bitwarden item UUID
        key: REPLACE_WITH_BITWARDEN_ITEM_UUID
        property: BOT_APP_PRIVATE_KEY
---
# yaml-language-server: $schema=https://kube-schemas.pages.dev/generators.external-secrets.io/githubaccesstoken_v1alpha1.json
# Mints short-lived installation token from the App PEM above
apiVersion: generators.external-secrets.io/v1alpha1
kind: GithubAccessToken
metadata:
  name: renovate-bot-auth-token
spec:
  # TODO: replace with actual GitHub App ID + Installation ID
  appID: "REPLACE_APP_ID"
  installID: "REPLACE_INSTALL_ID"
  auth:
    privateKey:
      secretRef:
        name: renovate-bot-pem
        key: private_key_pem
---
# yaml-language-server: $schema=https://kube-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
# Final secret consumed by RenovateJob.spec.secretRef
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: &name renovate-bot-auth-token
spec:
  refreshInterval: 30m
  target:
    name: *name
    template:
      type: Opaque
      engineVersion: v2
      data:
        GITHUB_COM_TOKEN: "{{ .token }}"
        # TODO: confirm bot user login (matches App slug + [bot] suffix usually)
        GITHUB_COM_USER: profmoo-bot
        RENOVATE_TOKEN: "{{ .token }}"
  dataFrom:
    - sourceRef:
        generatorRef:
          apiVersion: generators.external-secrets.io/v1alpha1
          kind: GithubAccessToken
          name: renovate-bot-auth-token
---
# yaml-language-server: $schema=https://kube-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
# Docker Hub creds for Renovate dependency lookups against docker.io
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: &name docker-hub-token
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden-fields
  target:
    name: *name
    template:
      data:
        DOCKERHUB_USERNAME: "{{ .DOCKERHUB_USERNAME }}"
        DOCKERHUB_TOKEN: "{{ .DOCKERHUB_TOKEN }}"
  data:
    - secretKey: DOCKERHUB_USERNAME
      remoteRef:
        # TODO: replace with actual Bitwarden item UUID
        key: REPLACE_WITH_BITWARDEN_ITEM_UUID
        property: DOCKERHUB_USERNAME
    - secretKey: DOCKERHUB_TOKEN
      remoteRef:
        key: REPLACE_WITH_BITWARDEN_ITEM_UUID
        property: DOCKERHUB_TOKEN
