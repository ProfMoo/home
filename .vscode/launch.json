{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceRoot}/uploader/main.go",
            "cwd": "${workspaceRoot}/uploader",
            "console": "integratedTerminal"
        }
    ]
}
